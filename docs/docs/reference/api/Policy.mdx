---
title: "Policy"
description: "Action-level autonomy policy: which actions may run, and under which conditions."
---

{/* generated by tools/docs_audit/render_api.py from the docstrings — edit the docstring, never this page */}

`ctrlrun.Policy` — class, defined at `src/ctrlrun/policy.py:575`

```python
from ctrlrun import Policy
```


```python
class Policy
    def __init__(actions: Mapping[str, _ActionPolicy], source: str, schema: str = POLICY_SCHEMA, environment: str | None = None, mode: Literal['observe', 'enforce'] = ENFORCE, version: str | None = None, controls: Mapping[str, PolicyControl] = dict(), _canonical: Mapping[str, Any] = dict())
```

Action-level autonomy policy: which actions may run, and under which conditions.

Load with `Policy.from_file()`. A policy that cannot be loaded is an error, never an
empty permissive policy (SPEC-v0.1 §3.4).

## Next

- [Python API index](/docs/reference/api/index).
- [Get started](/docs/get-started/quickstart) · [Why](/docs/why).
